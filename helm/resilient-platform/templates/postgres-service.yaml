apiVersion: v1
kind: Service
metadata:
  name: {{ include "resilient-platform.fullname" . }}-{{ .Values.postgres.host }}-service
  labels:
    {{- include "resilient-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: {{ include "resilient-platform.fullname" . }}-{{ .Values.postgres.host }}
spec:
  type: ClusterIP
  selector:
    {{- include "resilient-platform.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: {{ include "resilient-platform.fullname" . }}-{{ .Values.postgres.host }}
  ports:
    - name: {{ include "resilient-platform.fullname" . }}-{{ .Values.postgres.host }}
      port: {{ .Values.postgres.port }}
      targetPort: {{ .Values.postgres.port }}
      protocol: TCP