apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "resilient-platform.fullname" . }}-test-connection"
  labels:
    {{- include "resilient-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "resilient-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
