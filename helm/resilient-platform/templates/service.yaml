apiVersion: v1
kind: Service
metadata:
  name: {{ include "resilient-platform.fullname" . }}
  labels:
    {{- include "resilient-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "resilient-platform.selectorLabels" . | nindent 4 }}
